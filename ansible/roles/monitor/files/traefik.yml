api:
  dashboard: false
entryPoints:
  http:
    address: ":80"
serversTransport:
  insecureSkipVerify: true
providers:
  docker:
    constraints: "Label(`traefik.zone.outpost`, `true`)"
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
log:
  level: INFO